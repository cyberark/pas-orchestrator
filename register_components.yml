---
# tasks file for register_components
- hosts: cpm
  serial: 1
  tasks:
  - name: Register cpm
    include_role: 
      name: cpm
    vars:
      cpm_registration: true
      
- hosts: pvwa
  serial: 1
  tasks:
  - name: Register pvwa
    include_role: 
      name: pvwa
    vars:
      pvwa_registration: true
      
- hosts: psm
  serial: 1
  tasks:
  - name: Register psm
    include_role: 
      name: psm
    vars:
      psm_registration: true
  