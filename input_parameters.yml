---
# file: import_parameters.yml
# - hosts: all
#   connection: local
#   gather_facts: false
  

  
  tasks:
  
    - set_fact:
        components_registration_order: 
          # - cpm
          - pvwa
          # - psm
      when: version == "v10.6"
      
    - set_fact:
        components_registration_order: 
          - pvwa
          - cpm
          - psm
      when: version != "v10.6"
